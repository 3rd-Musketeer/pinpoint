# ROADMAP — pinpoint

五阶段收敛与扩展（2026-08-16 定稿，goal 驱动执行）。每阶段切开的位置按
「独立价值」划分，不按实现便利——每刀都要回答「为什么在这切」。

产品心智：pinpoint 是原型的引用系统与总线。开发发生在各处（文件留在原地），
内容经注册/代理进入 pinpoint；同一对象有两种形态——画布（迭代态）与文档
（表达态）；标注绑定对象不绑定视图，两个面透传。

## 阶段 1：画布呼吸空间（侧栏宽度 V2）

- 价值：标注/对比时画布不再被两侧栏挤死——现在每次用都碰到的不舒服。
- 切开理由：纯外壳修正，不碰对象模型，风险最小、见效最快，不等任何后续决策。
- 内容：右栏 splitter（260–440，默认 308）+ 过 280 紧凑断点（藏 cap、单行
  truncate、底栏 dropdown 收图标+值）+ 双击复位 + 宽度持久化。左栏维持现状
  （已有 200–480 + 折叠）。rail 档、窗口自动让位：确认不做。
- 契约：体验板 `previews/sidebar-variants/sidebar.html`「侧栏宽度：分析 +
  方案」V2 mock。

## 阶段 2：对象模型收敛（Web 退役 + Pages 统一）

- 价值：Pages = 一个列表装所有原型对象；「画布 / 文档」两形态心智落地；
  my-todos 这类条目不再藏 Web tab 下。
- 切开理由：后面所有「入口」工作的地基——不先收敛，每个新入口都要回答
  「我属于哪个模式」的假问题。先收敛心智，再加内容。
- 内容：删 web 模式 + 裸画板壳（wrapHtmlShell / WEB_LIB_ID / 模式分流），
  Seg 退役，Pages 单一列表 + 行内壳标记（机壳/文档），Component Library 作
  系统行保留；web-library 示例页删除；eval-queue-variants 迁 doc 壳；
  registry dir 条目默认壳改 doc。壳仍是页/帧属性；html 页打开 stage 变
  阅读器的行为不变。

## 阶段 3：注册入口（CLI）

- 价值：pinpoint 的边界从 previews 目录扩到任何项目——agent 在别的项目写完
  原型，一条命令登记，文件留在原地，出现在 Pages 里可被标注。
- 心智：想让一个页面进 pinpoint 就用 CLI；静态的 pinpoint 直接 host（/sites/
  现有管道），活的登记 URL 待阶段 4 映射。N 个项目小服务器 → 一台总线 +
  一张登记表。
- 切开理由：第一个内容入口，改变工具服务范围；薄而独立，不依赖代理。

## 阶段 4：live 入口（代理画中画）

- 价值：活服务器页面进 workbench，标注不再只能靠浏览器扩展——侧栏直接驱动，
  和原型同一个工作台。
- 切开理由：静态内容与活内容成本、触发条件不同；由 my-todos 真实标注需求
  驱动立项。
- 内容：同源代理（含 WebSocket 转发）+ registry url 条目以 doc 壳内嵌 +
  代理层注入 annotate.js。取代 backlog 的「跨域 iframe + postMessage 桥」
  路线（同源代理使桥不必要）。

## 阶段 5：表达态（文档 mention 活 frame + 标注透传）

- 价值：迭代工具 → 迭代 + 表达工具。设计文档引用活原型（不是截图、不是副本，
  是活对象的引用）；标注跨画布/文档两个面透传。
- 切开理由：唯一创造新对象关系（文档引用 frame）的阶段；依赖阶段 2 的对象
  模型稳定（frame 身份是透传地基），被阶段 3/4 的内容流入放大。
- 语义：doc 页 mention 画布 frame → 水合为活 DOM（可交互，与画布同一份
  fragment）；标注双向透传（同一份存储、两处渲染、实时同步）；文档导出时
  frame 烤成静态图；不做冻结/REV。mention 语法、样式隔离、模式路由由执行
  agent 决定，产品语义以本段为准。

## 缓期（`.gdd/backlog.md`，owner-local）

REV 版本机制（启动信号：异步多轮验收成常态，或文档外发使版本对齐变真痛点）、
批注烘进 frame 图片导出（启动信号：带批注帧图外发需求）、frame 菜单 hit-test
flake 根治等。
